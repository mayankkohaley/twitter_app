<h1>Sign in</h1>

<%= form_for(:session, :url => sessions_path) do |f| %>
  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :password %><br />
    <%= f.password_field :password %>
  </div>
  <div class="actions">
    <%= button_submit_tag 'Submit' %>
  </div>
<% end %>

<p>New user? <%= add_button_link_to "Sign up now!", signup_path %></p>
